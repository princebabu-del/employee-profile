{
  "name": "employee-profile",
  "version": "0.1.0",
  "private": true,
  "description": "Lumber Users / Employee Profile page",
  "scripts": {
    "start": "live-server --port=3000 --open=/index.html",
    "dev": "live-server --port=3000 --open=/index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
